<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Postmedia</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 text-white overflow-hidden flex">

      <div
        class="w-1/2 h-screen overflow-hidden flex justify-center items-center max-[440px]:hidden"
      >
        <img
          class="object-cover object-center hover:shadow-xl rounded-lg"
          src="/images/landing.jpg"
          alt=""
        />
      </div>

      <div
        class="w-1/2 h-screen flex flex-col justify-center bg-blue-300 items-center max-[440px]:w-full"
      >
        <h1 class="text-5xl text-blue-800">Sign In</h1>
        <form
          action="/login"
          method="post"
          class="w-[30vw] h-[60vh] flex flex-col gap-10 py-10 justify-center items-center max-[440px]:text-xl relative"
        >
          <div>
          
          <div>
            <label for="username">Username</label>
            <input
              class="w-[30vw] border-none bg-zinc-600 p-3 rounded-lg mt-2 max-[440px]:w-[80vw]"
              type="text"
              name="username"
              id=""
              placeholder="username"
            />
          </div>
          <div>
            <label for="Email">Email</label>
            <input
              class="w-[30vw] border-none bg-zinc-600 p-3 rounded-lg mt-2 max-[440px]:w-[80vw]"
              type="email"
              name="email"
              id=""
              placeholder="Email.."
            />
          </div>
          <div class="mt-5">
            <label for="Password">Password</label>
            <input
              class="w-[30vw] border-none bg-zinc-600 p-3 rounded-lg mb-4 max-[440px]:w-[80vw]"
              type="password"
              name="password"
              id=""
              placeholder="Password.."
            />
          </div>
          <div>
            <input
              class="bg-blue-800 mt-5 w-[30vw] h-[6vh] mb-8 hover:cursor-pointer text-white rounded-lg max-[440px]:w-[80vw]"
              type="submit"
              value="Sign In"
            />
            
          </div>

          <div class="w-full flex justify-center items-center gap-6">
            <a href="/" class="text-blue-700 underline underline-offset-2">
             Don't have an account ? Create Account
            </a>
          </div>
       
        </form>
      </div>
    </div>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
