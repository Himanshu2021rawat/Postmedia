<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="w-full h-screen bg-zinc-900 text-white flex justify-center items-center"
    >
      <form
        action="/update/<%=post._id%>"
        method="post"
        class="flex gap-3 max-[440px]:p-5 max-[440px]:flex-col max-[440px]:w-[90vw] max-[440px]:h-[50vh] max-[440px]:border-2 max-[440px]:border-zinc-600 max-[440px]:rounded-lg"
      >
        <textarea
          class="w-[30vw] bg-zinc-700 p-2 rounded-lg resize-none max-[440px]:w-full max-[440px]:h-[30vh] max-[440px]:text-2xl max-[440px]:p-4"
          name="content"
          placeholder="Edit your post"
          id=""
        >
<%=post.content%></textarea
        >
        <input
          class="w-[8vw] bg-yellow-500 rounded-lg cursor-pointer max-[440px]:w-1/2 max-[440px]:p-4 max-[440px]:text-xl"
          type="submit"
          value="Update Post"
        />

        <a
          href="/logout"
          class="w-1/6 flex justify-center items-center bg-red-500 rounded-lg max-[440px]:p-4 max-[440px]:w-1/2 max-[440px]:text-xl"
          >LogOut</a
        >
      </form>
    </div>
  </body>
</html>
